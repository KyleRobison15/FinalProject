import { FilterIncomingExchangesPipe } from './filter-incoming-exchanges.pipe';

describe('FilterIncomingExchangesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterIncomingExchangesPipe();
    expect(pipe).toBeTruthy();
  });
});
