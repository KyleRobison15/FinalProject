import { FilterExchangesInProgressPipe } from './filter-exchanges-in-progress.pipe';

describe('FilterExchangesInProgressPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterExchangesInProgressPipe();
    expect(pipe).toBeTruthy();
  });
});
