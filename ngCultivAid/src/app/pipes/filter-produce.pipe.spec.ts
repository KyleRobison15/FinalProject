import { FilterProducePipe } from './filter-produce.pipe';

describe('FilterProducePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProducePipe();
    expect(pipe).toBeTruthy();
  });
});
