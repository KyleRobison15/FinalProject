import { SortRecentPipe } from './sort-recent.pipe';

describe('SortRecentPipe', () => {
  it('create an instance', () => {
    const pipe = new SortRecentPipe();
    expect(pipe).toBeTruthy();
  });
});
