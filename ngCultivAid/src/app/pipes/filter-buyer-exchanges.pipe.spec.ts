import { FilterBuyerExchangesPipe } from './filter-buyer-exchanges.pipe';

describe('FilterBuyerExchangesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBuyerExchangesPipe();
    expect(pipe).toBeTruthy();
  });
});
