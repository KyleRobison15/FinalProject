import { SortMessagePipe } from './sort-message.pipe';

describe('SortMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new SortMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
