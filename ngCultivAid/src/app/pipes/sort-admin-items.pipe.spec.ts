import { SortAdminItemsPipe } from './sort-admin-items.pipe';

describe('SortAdminItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortAdminItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
